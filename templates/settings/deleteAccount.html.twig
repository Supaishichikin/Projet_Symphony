{% extends 'base.html.twig' %}
{% block title %}Supprimer votre compte - {{ parent() }}{% endblock %}
{% block body %}
    <h1>Suppression du compte</h1>
    <p>
        Attention, la suppression sera effectuée après la validation de ce formulaire et est complètement irréversible.
    </p>
    <form method="post">
        <div class="form-group">
            <label for="checkPassword">Mot de passe</label>
            <input type="password" id="checkPassword" name="checkPassword" class="form-control">
        </div>
        <a href="{{ path('app_settings_index') }}" class="btn btn-secondary">Retour</a>
        <button type="submit" class="btn btn-danger float-right">Confirmer la suppression</button>
    </form>
{% endblock %}



