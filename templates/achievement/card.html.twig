<div class="col-4 text-center d-flex justify-content-center">
    <div class="card mb-3" style="width: 18rem;">
        <img src="http://via.placeholder.com/200x150" class="card-img-top" alt="...">
        <div class="card-body">
            <h5 class="card-title">{{ achievement.name }}</h5>
            <p class="card-text">{{ achievement.description}}</p>
            <a href="{{ path('app_achievement_detail', {'id': achievement.id}) }}" class="btn btn-primary">Voir détail</a>
            <br>
            <br>
            <a href="{{ path('app_achievement_processachievement', {'id': achievement.id}) }}" class="btn {{ achievement.status.class }}">{{ achievement.status.message }}</a>
        </div>
    </div>
</div>